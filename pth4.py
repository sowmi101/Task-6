# Define an example array (list)
example_array = [5, 2, 9, 1, 5, 6]

# Initialize a variable to store the sum
total_sum = 0

# Iterate through the elements of the array and add them to the sum
for element in example_array:
    total_sum += element

# Print the total sum
print("Sum of array elements:", total_sum)
